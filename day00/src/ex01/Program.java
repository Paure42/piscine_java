public class Program {
    public static void main(String[] args) {
        if (args.length < 1)
            System.exit(0);
       int number = (args[0]);
        System.out.println(number);
    }
}
